#
# Copyright 2026 The OKDP Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS,
#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#  See the License for the specific language governing permissions and
#  limitations under the License.
#

# Consumes one database-server Connection (parameter db). Publishes none.

apiVersion: v1alpha1
name: keycloak
tag: 24.4.11-p12
protected: false
description: |
  Keycloak identity and access management for Kubernetes
usage:
  text: |
    Keycloak provides centralized authentication and authorization.
    Access the admin console at https://keycloak.{{ .Context.ingress.suffix }}
    Default admin credentials: admin/admin
schema:
  parameters:
    properties:
      adminUser:
        type: string
        default: "admin"
        description: "Keycloak admin username"
      adminPassword:
        type: string
        default: "admin"
        description: "Keycloak admin password"
      memoryGi:
        type: number
        default: 2
        multipleOf: 0.25
        description: "Memory (GiB) used for Kubernetes requests. Limits are calculated as 2x the request."
      cpu:
        type: number
        default: 0.5
        multipleOf: 0.25
        description: "CPU request in vCPU. Limits are calculated as 2x the request."
      db: { type: connectionRef, contract: database-server, required: true }
      realm:
        properties:
          name: { type: string, required: true }
          displayName: { type: string, default: "" }
          accessTokenLifespan: { type: integer, default: 3600 }
      # DCR callers must be covered by trustedHosts, their extra scopes by
      # allowedScopes. Defaults live in the template, the schema forbids them
      # on arrays: ["*.<ingress suffix>", "*.svc.cluster.local"] / ["groups"].
      anonymousDCR:
        properties:
          trustedHosts:
            type: array
            items: { type: string }
          allowedScopes:
            type: array
            items: { type: string }
      users:
        type: array
        items:
          properties:
            username: { type: string, required: true }
            email: { type: string }
            firstName: { type: string }
            lastName: { type: string }
            password: { type: string }
            serviceAccountClientId: { type: string }
            roles:
              type: array
              items: { type: string }
      groups:
        type: array
        items:
          properties:
            name: { type: string, required: true }
      roles:
        type: array
        items:
          properties:
            name: { type: string, required: true }
      clients:
        type: array
        items:
          properties:
            clientId: { type: string, required: true }
            name: { type: string }
            publicClient: { type: boolean }
            secret: { type: string }
            redirectUris:
              type: array
              items: { type: string }
            webOrigins:
              type: array
              items: { type: string }
            defaultClientScopes:
              type: array
              items: { type: string }
            optionalClientScopes:
              type: array
              items: { type: string }
            serviceAccountsEnabled: { type: boolean }
            standardFlowEnabled: { type: boolean }
            implicitFlowEnabled: { type: boolean }
            directAccessGrantsEnabled: { type: boolean }
      clientScopes:
        type: array
        items:
          properties:
            name: { type: string, required: true }
            description: { type: string }
            protocol: { type: string }
            attributes: { type: object, properties: {}, additionalProperties: true }
            protocolMappers:
              type: array
              items:
                properties:
                  name: { type: string, required: true }
                  protocol: { type: string }
                  protocolMapper: { type: string }
                  config: { type: object, properties: {}, additionalProperties: true }

modules:
  - name: main
    timeout: 10m
    source:
      oci:
        repository: registry-1.docker.io/bitnamicharts/keycloak
        tag: 24.4.11
    values: |

      {{- $dcr := (.Parameters.anonymousDCR | default dict) -}}
      {{- $dcrTrustedHosts := ($dcr.trustedHosts | default (list (printf "*.%s" $.Context.ingress.suffix) "*.svc.cluster.local")) -}}
      {{- $dcrAllowedScopes := ($dcr.allowedScopes | default (list "groups")) -}}

      global:
        security:
          allowInsecureImages: true
      image:
        registry: quay.io
        repository: okdp/sandbox-images/keycloak
        tag: 26.1.3-debian-12-r0
      ingress:
        enabled: true
        ingressClassName: "nginx"
        annotations:
          nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
          nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
          cert-manager.io/cluster-issuer: "{{ .Context.certificateIssuers.selfSigned.name }}"
        tls: true
        hostname: keycloak.{{ .Context.ingress.suffix }}

      auth:
        adminUser: "{{ .Parameters.adminUser }}"
        adminPassword: "{{ .Parameters.adminPassword }}"

      postgresql:
        enabled: false
      externalDatabase:
        host: "{{ .Parameters.db.host }}"
        port: "{{ .Parameters.db.port }}"
        database: {{ .Parameters.db.dbName }}
      extraEnvVars:
      - name: KC_DB
        value: 'postgres'
      - name: KC_DB_URL
        value: 'jdbc:{{ .Parameters.db.engine }}://{{ .Parameters.db.host }}:{{ .Parameters.db.port }}/{{ .Parameters.db.dbName }}'
      - name: KC_DB_USERNAME
        valueFrom:
          secretKeyRef:
            name: {{ .Parameters.db.secretRef }}
            key: username
      - name: KC_DB_PASSWORD
        valueFrom:
          secretKeyRef:
            name: {{ .Parameters.db.secretRef }}
            key: password
      - name: KC_METRICS_ENABLED
        value: "false"
      - name: KC_TRACING_ENABLED
        value: "false"
      - name: KC_CACHE
        value: "local"

      production: false
      cache:
        enabled: false

      resources:
        requests:
          cpu: "{{ mulf (float64 .Parameters.cpu) 1000 }}m"
          memory: "{{ .Parameters.memoryGi }}Gi"
        limits:
          cpu: "{{ mulf (float64 .Parameters.cpu) 2 }}"
          memory: "{{ mulf (float64 .Parameters.memoryGi) 2 }}Gi"

      keycloakConfigCli:
        image:
          registry: quay.io
          repository: okdp/sandbox-images/keycloak-config-cli
          tag: 6.4.0-debian-12-r0
        enabled: true
        configuration:
          master.json: |
            {
              "realm": "{{ .Parameters.realm.name }}",
              "enabled": true,
              "displayName": "{{ .Parameters.realm.displayName }}",
              "users": [
                {{- range $index, $user := .Parameters.users }}
                {{- if $index }},{{ end }}
                {
                  "username": "{{ $user.username }}",
                  "enabled": true,
                  "emailVerified": true,
                  "email": "{{ $user.email }}",
                  "firstName": "{{ $user.firstName }}",
                  "lastName": "{{ $user.lastName }}",
                  {{- if $user.password }}
                  "credentials": [
                    {
                      "type": "password",
                      "value": "{{ $user.password }}"
                    }
                  ],
                  {{- end }}
                  {{- if $user.serviceAccountClientId }}
                  "serviceAccountClientId": "{{ $user.serviceAccountClientId }}",
                  {{- end }}
                  "realmRoles": {{ toJson $user.roles }}
                }
                {{- end }}
              ],
              "groups": [
                {{- range $index, $group := .Parameters.groups }}
                {{- if $index }},{{ end }}
                {
                  "name": "{{ $group.name }}"
                }
                {{- end }}
              ],
              "roles": {
                "realm": [
                  {{- range $index, $role := .Parameters.roles }}
                  {{- if $index }},{{ end }}
                  {
                    "name": "{{ $role.name }}"
                  }
                  {{- end }}
                ]
              },
              "clients": [
                {{- range $index, $client := .Parameters.clients }}
                {{- if $index }},{{ end }}
                {
                  "clientId": "{{ $client.clientId }}",
                  "name": "{{ $client.name }}",
                  {{- if $client.publicClient }}
                  "publicClient": {{ $client.publicClient }},
                  {{- else }}
                  "secret": "{{ $client.secret }}",
                  {{- end }}
                  "redirectUris": [
                    {{- range $uriIndex, $uri := $client.redirectUris }}
                    {{- if $uriIndex }},{{ end }}
                    "{{ $uri }}"
                    {{- end }}
                  ],
                  "webOrigins": [
                    {{- range $originIndex, $origin := $client.webOrigins }}
                    {{- if $originIndex }},{{ end }}
                    "{{ $origin }}"
                    {{- end }}
                  ]
                  {{- if $client.defaultClientScopes }},
                  "defaultClientScopes": {{ toJson $client.defaultClientScopes }}
                  {{- end }}
                  {{- if $client.optionalClientScopes }},
                  "optionalClientScopes": {{ toJson $client.optionalClientScopes }}
                  {{- end }}
                  {{- if $client.serviceAccountsEnabled }},
                  "serviceAccountsEnabled": {{ $client.serviceAccountsEnabled }}
                  {{- end }}
                  {{- if hasKey $client "standardFlowEnabled" }},
                  "standardFlowEnabled": {{ $client.standardFlowEnabled }}
                  {{- end }}
                  {{- if hasKey $client "implicitFlowEnabled" }},
                  "implicitFlowEnabled": {{ $client.implicitFlowEnabled }}
                  {{- end }}
                  {{- if hasKey $client "directAccessGrantsEnabled" }},
                  "directAccessGrantsEnabled": {{ $client.directAccessGrantsEnabled }}
                  {{- end }}
                }
                {{- end }}
              ],
              "clientScopes": [
                {{- range $index, $scope := .Parameters.clientScopes }}
                {{- if $index }},{{ end }}
                {
                  "id": "{{ $scope.name }}-scope-id",
                  "name": "{{ $scope.name }}",
                  "description": "{{ $scope.description }}",
                  "protocol": "{{ $scope.protocol }}",
                  "attributes": {
                    {{- $attrKeys := keys $scope.attributes | sortAlpha }}
                    {{- range $attrIndex, $key := $attrKeys }}
                    {{- if $attrIndex }},{{ end }}
                    "{{ $key }}": "{{ index $scope.attributes $key }}"
                    {{- end }}
                  },
                  "protocolMappers": [
                    {{- range $mapperIndex, $mapper := $scope.protocolMappers }}
                    {{- if $mapperIndex }},{{ end }}
                    {
                      "id": "{{ $mapper.name }}-mapper-id",
                      "name": "{{ $mapper.name }}",
                      "protocol": "{{ $mapper.protocol }}",
                      "protocolMapper": "{{ $mapper.protocolMapper }}",
                      "consentRequired": false,
                      "config": {
                        {{- $configKeys := keys $mapper.config | sortAlpha }}
                        {{- range $configIndex, $key := $configKeys }}
                        {{- if $configIndex }},{{ end }}
                        "{{ $key }}": "{{ index $mapper.config $key }}"
                        {{- end }}
                      }
                    }
                    {{- end }}
                  ]
                }
                {{- end }}
              ],
              "accessTokenLifespan": {{ .Parameters.realm.accessTokenLifespan }},
              "components": {
                "org.keycloak.services.clientregistration.policy.ClientRegistrationPolicy": [
                  {
                    "name": "Trusted Hosts",
                    "providerId": "trusted-hosts",
                    "subType": "anonymous",
                    "config": {
                      "host-sending-registration-request-must-match": ["true"],
                      "client-uris-must-match": ["true"],
                      "trusted-hosts": ["{{ $dcrTrustedHosts | join "\", \"" | replace "{{ .Context.ingress.suffix }}" $.Context.ingress.suffix }}"]
                    }
                  },
                  {
                    "name": "Allowed Client Scopes",
                    "providerId": "allowed-client-templates",
                    "subType": "anonymous",
                    "config": {
                      "allow-default-scopes": [
                        "true"
                      ],
                      "allowed-client-scopes": [
                        "{{ $dcrAllowedScopes | join "\", \"" }}"
                      ]
                    }
                  }
                ]
              }
            }
roles:
  - identity
dependencies:
  - external-secrets
  - database-server
  - ingress
# No outputs: the OIDC coordinates are platform Context values, not a
# connection one service would provide to the others.
